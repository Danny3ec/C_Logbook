#include <stdio.h> // we need this library to get access to the input and output
int main()
{
    printf("Hello World\n"); // lets say hello, where it all began...
    printf("Goodbye World\n"); // this seems fitting as the program will close after
    return 0; //returns 0 to teh int of main() and terminate the progragclsls